// AI confidence score for this refactoring: 60.91%
export interface CustomView {
    // id: number; // Uncomment this line if id is needed
    calendarName: string;
    branches: string[];
    isFavorite: boolean;
    isDefault: boolean;
}

/*
- The id property is commented out but should ideally be handled according to its usage.
- There are mixed formatting styles (comma at the end of the last property can be omitted).
- Consistent use of semicolons is not followed.
*/